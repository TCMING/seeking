# Q2: 如何处理高并发场景？

> ibrain-gpt-backend 日均百万级请求，三层治理：
>
> 1. **接入层**：令牌桶限流 + 三层访问控制（用户/应用/接口）快速拒绝超额请求；
> 2. **缓存层**：Redis 热点数据 + 本地 Caffeine 多级缓存，减少 DB 压力；
> 3. **依赖层**：Hystrix 熔断隔离 + 超时降级保护下游；
> 4. **数据层**：ShardingSphere 读写分离缓解主库压力。
>
> 最终在外部依赖波动场景下也能稳定支撑百万级日请求。
