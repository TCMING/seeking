# 幂等设计 ⭐

## 项目应用

`midjourney-proxy` 异步任务幂等提交；消息消费幂等。

## 核心考点

- 幂等性定义：同一操作执行多次结果一致
- 实现方式：
  1. 唯一 ID + 去重表（最通用）
  2. Redis SETNX（轻量）
  3. 状态机（更新带状态前置条件）
  4. 数据库唯一索引（最终防线）
  5. 乐观锁（version 字段）
- HTTP 幂等性：GET / PUT / DELETE 幂等，POST 非幂等
- 消息消费幂等：业务层去重 vs MQ 去重
